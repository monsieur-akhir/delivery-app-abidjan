# === Informations générales ===
DEBUG=True
ENVIRONMENT=development
SECRET_KEY=dev-secret-key

# === Base de données (PostgreSQL local) ===
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/livraison_abidjan
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=livraison_abidjan

# === Redis local ===
REDIS_URL=redis://localhost:6379/0

# === Keycloak (local) ===
KEYCLOAK_URL=http://localhost:8080/auth
KEYCLOAK_REALM=livraison-abidjan
KEYCLOAK_CLIENT_ID=backend
KEYCLOAK_CLIENT_SECRET=dev-client-secret
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
# === Keycloak (Docker) ===


# === CinetPay ===
CINETPAY_API_KEY=dev-api-key
CINETPAY_SITE_ID=dev-site-id
CINETPAY_SECRET_KEY=dev-secret-key
CINETPAY_RETURN_URL=http://localhost:3000/payment/success
CINETPAY_CANCEL_URL=http://localhost:3000/payment/cancel

# === Twilio (bac à sable) ===
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+1234567890

# === OneSignal ===
ONESIGNAL_APP_ID=dev-onesignal-app-id
ONESIGNAL_API_KEY=dev-onesignal-api-key

# === OpenWeatherMap ===
OPENWEATHERMAP_API_KEY=dev-openweathermap-api-key

# === Google Maps API ===
GOOGLE_PLACES_API_KEY=AIzaSyDZPEGgSOg1dbSBP2Al26CMrN88haV43mI

# === Email Configuration ===
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=noreply@livraison-abidjan.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@livraison-abidjan.com

# === Brevo Configuration (Email & SMS) ===

BREVO_ENABLED=True
BREVO_FROM_EMAIL=noreply@livraison-abidjan.com
BREVO_FROM_NAME=Livraison Abidjan
BREVO_MAIL_API_URL=https://api.brevo.com/v3/smtp/email
BREVO_SMS_API_URL=https://api.brevo.com/v3/transactionalSMS/sms

# === Email Configuration ===
EMAIL_ENABLED=True
EMAIL_PROVIDER=brevo
EMAIL_SENDER_ADDRESS=royalwebdevci@gmail.com
EMAIL_SENDER_NAME=Royal Tech

MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USER=8adce7001@smtp-brevo.com
MAIL_PASSWORD=VmYfMgBPWJGIjc2k
MAIL_FROM=8adce7001@smtp-brevo.com
MAIL_TRANSPORT=smtp://${MAIL_USER}:${MAIL_PASSWORD}@${MAIL_HOST}:${MAIL_PORT}

# === SMS Configuration ===
SMS_ENABLED=True
SMS_PROVIDER=brevo
SMS_SENDER=LiverCI
SMS_SENDER_ID=LiverCI
SMS_DAILY_LIMIT=1000

# === OTP Configuration ===
OTP_EXPIRY_MINUTES=5
OTP_LENGTH=6
OTP_MAX_ATTEMPTS=3

# === SMTP Configuration (Fallback) ===
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=noreply@livraison-abidjan.com
SMTP_PASSWORD=your-app-password

# === Notification Configuration ===
NOTIFICATION_ENABLED=True

# === Rasa ===
RASA_URL=http://localhost:5005